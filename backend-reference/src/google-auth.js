const crypto=require('crypto');
let cached=null;
function b64url(v){return Buffer.from(v).toString('base64url');}
async function accessToken(){if(cached&&cached.exp>Date.now()+60000)return cached.token;const email=process.env.GOOGLE_SERVICE_ACCOUNT_EMAIL||'';const key=(process.env.GOOGLE_PRIVATE_KEY||'').replace(/\\n/g,'\n');if(!email||!key)throw new Error('Google Sheets credentials are not configured');const now=Math.floor(Date.now()/1000),header=b64url(JSON.stringify({alg:'RS256',typ:'JWT'}));const claim=b64url(JSON.stringify({iss:email,scope:'https://www.googleapis.com/auth/spreadsheets',aud:'https://oauth2.googleapis.com/token',iat:now,exp:now+3600}));const unsigned=header+'.'+claim,sign=crypto.createSign('RSA-SHA256');sign.update(unsigned);sign.end();const jwt=unsigned+'.'+sign.sign(key).toString('base64url');const body=new URLSearchParams({grant_type:'urn:ietf:params:oauth:grant-type:jwt-bearer',assertion:jwt});const r=await fetch('https://oauth2.googleapis.com/token',{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body});if(!r.ok)throw new Error('Google authentication failed'); const j=await r.json();cached={token:j.access_token,exp:Date.now()+Number(j.expires_in||3600)*1000};return cached.token;}
module.exports={accessToken};
